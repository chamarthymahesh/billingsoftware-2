version: "3.8"

services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "5000:8080"
    environment:
      - PORT=8080
      - MONGO_URI=mongodb+srv://testbilling:Nehaal2026@testbilling.nru5qp8.mongodb.net/billbook_gst
      - JWT_SECRET=testsecretkey12345
    restart: always

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=http://server:8080
    ports:
      - "5173:80"
    depends_on:
      - server
    restart: always
